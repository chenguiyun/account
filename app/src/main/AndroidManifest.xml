<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chen.com.myaccount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".OutComeActivity"
            android:label="@string/title_activity_out_come"
            android:theme="@style/AppTheme.NoActionBar">
            >
        </activity>
        <activity android:name=".OutComeAddActivity" />
        <activity android:name=".Home">

        </activity>
        <activity android:name=".OutcomeeditsActivity" />
        <activity android:name=".AccountFlag" />
        <activity android:name=".FlagManage" />
        <activity android:name=".ShowInfo" />
        <activity android:name=".IndexActivity" />
        <activity android:name=".OutActivity" />
        <activity android:name=".IncomeaddActivity" />
        <activity android:name=".IncomeeditActivity"></activity>
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>